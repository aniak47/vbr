
<% provide(:title, 'Sign up') %>


<div class="row">
  <div class="wapper col-md-6 col-md-offset-3">
    <h3> Add new staff </h3> <hr>
       <%= form_for(@staff) do |f| %>
       <%= render 'shared/error_messages', object: f.object  %>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>
      
      <%= f.hidden_field :joktype, :value => 'none' %>

      <%= f.hidden_field :password, :value => 'bb39fn3o4bfo2euofboub' %>

      <%= f.hidden_field :password_confirmation, :value => 'bb39fn3o4bfo2euofboub' %>

      <%= f.submit "Create User", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
